#pragma once

namespace sgl {
namespace v1 {
// TODO: FINISHME

/*
template<typename It0, typename Out0, typename Out1>
void radix_sort(It0 first0 , It0 last0, Out0 out0, Out1 buffer) {

}

template<typename T, typename A>
struct sorted_array {
    sgl::v1::array<T, A> value;

    sorted_array() = default;
    sorted_array(const sorted_array&) = default;

    template<typename It>
    sorted_array(It first, size_t n) : (n + 257 * sizeof(T)) {
        auto out = value.begin();
        auto bits_hierarchy = out + n;
    }
};
*/


} // namespace v1
} // namespace sgl
